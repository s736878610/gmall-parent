server:
  port: 8202

spring:
  # nacos地址
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.200.128
  # 服务名
  application:
    name: service-item
  #redis配置
  redis:
    host: 192.168.200.128
    port: 6379

# feign超时设置
feign:
  client:
    config:
      default:
        connectTimeout: 600000
        readTimeout: 600000